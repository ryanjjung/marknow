{%- macro get_stylesheet() -%}
  styles/{{ style }}.css
{%- endmacro -%}

<!DOCTYPE html>
<html>
<head>
  <title>{{request.path.split('/')[-1]}}</title>
{%- if refresh_seconds != None %}
  <meta http-equiv="refresh" content="{{refresh_seconds}}" />
{% endif %}
  <link rel="stylesheet" href="{{ url_for('static',filename=get_stylesheet()) }}">
</head>
<body><div class="content">
{% block content %}{% endblock %}
</div></body>
</html>
